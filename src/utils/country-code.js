/*
 * [a-zA-Z]   // From a to z (case-insensitive)
 * {2}   // Repeated 2 times
 */
// This regex is exported as a string, because it is used to control the Router path variable.
export default "[a-zA-Z]{2}";
